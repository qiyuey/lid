interface:
  display_name: "Lid Release"
  short_description: "Tag self-signed Lid releases via GitHub Actions"
  default_prompt: "Use $lid-release to tag the next self-signed Lid release and update Homebrew."
